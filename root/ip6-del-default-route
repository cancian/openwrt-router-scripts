ip -6 route del default

#ip route del default
#ip route add default dev br-lan
#ip route del default
#ip route add default dev eth0.2
#ip -6 route del default
#ip -6 route add default dev eth0.2
#route -A inet6 add default dev eth0.2
#ip route del default
#ip route add default dev br-lan
#ip route del default
#route -A inet6 add default dev br-lan
#route -A inet6 add default dev lo
#ip -6 route add default dev br-lan
#ip -6 route add default dev lo
#ping6 -I fe80::ee08:6bff:fe6d:a27e ipv6.google.com
#ip route del default
#ip route add default dev eth0.2
#ip -6 route del default
#ip -6 route add default dev eth0.2

